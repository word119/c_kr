#include<stdio.h>
#include<string.h>

#define MAXLINE 100

int main(int argc, char *argv[])
{

    int c;

    while (--argc > 0 && (*++argv)[0] == '-') {
        while (c = *++argv[0]) {
            switch (c) {
                case 'x':  
            }
        }
    }
}